/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GoalPipe } from './goal.pipe';

describe('GoalPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalPipe();
    expect(pipe).toBeTruthy();
  });
});
